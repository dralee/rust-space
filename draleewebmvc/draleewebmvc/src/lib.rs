/**
 * 库导出
 */
pub mod threadpool;
pub mod server;
pub mod httpcontext;
pub mod streamrw;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
